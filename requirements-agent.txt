# Runtime dependencies for the private biography-upload Agent pipeline.
# Keep OCR separate: scanned PDFs are explicitly reported as unsupported until
# a deployment provisions an OCR adapter and its language models.
pypdf==6.10.0
pdfplumber==0.11.9
